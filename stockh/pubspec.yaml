name: your_project_name
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.15.0-193.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
